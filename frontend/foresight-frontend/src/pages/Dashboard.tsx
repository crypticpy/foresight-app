/**
 * Dashboard page re-export.
 *
 * The 821-line monolith was decomposed into `pages/Dashboard/<section>`
 * modules behind this shim so consumers keep using
 * `from "./pages/Dashboard"` unchanged.
 */

export { default } from "./Dashboard/index";
