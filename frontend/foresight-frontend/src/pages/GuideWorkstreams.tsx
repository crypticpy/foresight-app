/**
 * GuideWorkstreams page re-export.
 *
 * The 1.95K-line monolith was decomposed into `pages/GuideWorkstreams/<feature>`
 * modules. This file preserves the public import surface so consumers
 * (currently App.tsx) keep using `from "./pages/GuideWorkstreams"` unchanged.
 */

export { default } from "./GuideWorkstreams/index";
