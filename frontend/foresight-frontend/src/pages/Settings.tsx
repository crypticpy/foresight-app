/**
 * Settings page re-export.
 *
 * The 629-line monolith was decomposed into `pages/Settings/<Card>` modules
 * behind this shim so consumers keep using `from "./pages/Settings"`
 * unchanged.
 */

export { default } from "./Settings/index";
