/**
 * Methodology page re-export.
 *
 * The 610-line monolith was decomposed into `pages/Methodology/<feature>`
 * modules. This file preserves the public import surface so consumers keep
 * using `from "./pages/Methodology"` unchanged.
 */

export { default } from "./Methodology/index";
