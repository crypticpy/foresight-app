/**
 * GuideDiscover page re-export.
 *
 * The 1683-line monolith was decomposed into `pages/GuideDiscover/<feature>`
 * modules. This file preserves the public import surface so consumers
 * (currently App.tsx) keep using `from "./pages/GuideDiscover"` unchanged.
 */

export { default } from "./GuideDiscover/index";
